
  <section class='row'>
  <div class="col-md-12 players-wrapper">
    <div class="players">
    <%= form_for(@game, html: {class: 'form'}) do |f| %>
<!--       <div class="form-group">
      <%#= f.label :player2_id, "Play against a guest" %><br>
      <%#= f.radio_button :player2_id, 1 %>
      </div> -->
      <div class="form-group">
        <%= f.label :player2_id, "Select opponent", :class => "opponent" %><br>
        <%= f.select :player2_id, @users.map {|c| [c.name, c.id] } %>
      </div>
      <p>Toss coin</p>
      <%= f.submit "Toss coin!", :type => :image, :src => "/coin.png", :class => "coin" %>
    <% end %>
</div>
</div>
</section>

